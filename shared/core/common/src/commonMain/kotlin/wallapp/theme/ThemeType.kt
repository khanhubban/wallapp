package wallapp.theme

/**
 * Denotes the user's selected Theme (either Light, Dark or System). Re #1466.
 *
 * See [ThemeDescriptor] for an actual theme definition.
 */
enum class ThemeType {
    Light,
    Dark,
    System,
}

