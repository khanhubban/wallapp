package wallapp.app

/**
 * Manages app transitions related to app launch.
 *
 * Currently, just used as a debug helper for jumping to a desired screen. Eventually, will be
 * used for loading tutorials, handling deep links, etc.
 */
interface AppLaunchManager {

    fun run()

}