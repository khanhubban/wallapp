package wallapp.image

import android.content.Context
import android.view.View

fun ImageVideoErrorView(context: Context): View {
    return View(context).apply {
        setBackgroundColor(0x88ee3e1c.toInt())
    }
}